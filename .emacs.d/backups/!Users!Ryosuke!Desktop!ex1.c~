/* L6.3 dynamics of procedure execution (call by value) */
int power(int x) {
  return (x * x);
}

main() {
  power(1 + 2 + 3);
}

/*
stack structure immediately after ``call power''

 esp -> -------------------------
        |    return address     |
        -------------------------
        |      06 00 00 00      |
        ------------------------- <- ebp
*/
